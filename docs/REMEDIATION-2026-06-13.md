# 审计整改跟踪（2026-06-13）

本文件逐条确认 `docs/AUDIT-2026-06-13.md` 与补充审核发现。状态说明：

- `已修复`：本轮已完成代码/迁移/测试整改。
- `部分修复`：已完成止血或低风险部分，仍需专项继续。
- `待专项`：需要较大架构改造、数据清洗、产品决策或更多测试基建，不应混入本轮止血补丁。

## 严重问题

| 编号 | 问题 | 状态 | 本轮措施 / 后续措施 |
| --- | --- | --- | --- |
| S1 | 作者可越权读取任意作者预发记录 | 已修复 | `listSiPreissues` 禁止作者端 `authorId` 覆盖本人过滤，并新增回归测试。 |
| S2 | `active_pair_key` 语义错误 | 已修复 | 活动绑定唯一键改为作者维度，新增迁移回填活动键。上线前需先清洗存量重复活动绑定。 |
| S3 | 修订模式 Delete 卡死 | 已修复 | Delete 后光标推进到标删区段之后，并跳过紧邻 deleted 文本。 |
| S4 | 缺少接受/拒绝修订机制 | 待专项 | 需要新增 revision accept/reject 命令、侧栏交互和提交前拦截，涉及编辑器核心状态机。 |
| S5 | replace 误把 inserted 标成 original | 部分修复 | 直接输入替换已分段处理；IME 替换已过滤 inserted slice。复杂跨块替换仍建议由统一事务过滤层覆盖。 |
| S6 | 结构性编辑绕过修订追踪 | 待专项 | 短期可禁用危险结构编辑；长期应引入 `filterTransaction/appendTransaction` 统一改写 docChanged 步骤。 |
| S7 | 移动端无导航入口 | 已修复 | 小屏汉堡按钮打开移动抽屉，复用同一份角色导航。 |
| S8 | 驳回页展示虚假管理员反馈 | 已修复 | 删除硬编码反馈文案，仅保留通用驳回说明。 |
| S9 | SI 主类型参数与表单脱节 | 已修复 | 新增 `/api/si-main-types` 只读接口，SI 表单和列表筛选改用启用主类型。 |
| S10 | 删除章节无确认弹窗 | 已修复 | 章节删除改为确认 Dialog，并展示章节标题和后果说明。 |
| S11 | 401 无全局处理、编辑侧路由缺守卫 | 部分修复 | `fetchJson` 统一 401 跳登录；`/si` 与 `/review` 增加服务端角色守卫。强制改密服务端守卫仍待后续用 middleware/header 方案完善。 |
| S12 | `ignoreBuildErrors: true` | 已修复 | 移除该配置，新增 `typecheck` 脚本；生产构建已确认会执行 TypeScript。 |

## 中等问题

| 编号 | 问题 | 状态 | 本轮措施 / 后续措施 |
| --- | --- | --- | --- |
| A1 | chapter 阶段计划永不 completed | 已修复 | 解锁质检时同步完成正文阶段计划。 |
| A2 | complete/archive/cancel 不清理打开待办，统计不过滤生命周期 | 部分修复 | 项目完成/归档/取消时取消打开待办；逾期统计过滤 active 项目。状态机完整性仍待 A5。 |
| A3 | 逾期同步在读路径全量扫描且并发通知 | 待专项 | 需改为定时任务和条件翻转，避免在本轮大范围改调用链。 |
| A4 | cleanText 校验被回退顺序架空 | 待专项 | 需服务端基于 contentJson 重导投影，并调整保存/提交校验。 |
| A5 | 治理生命周期缺状态机 | 待专项 | 需定义合法转移表和 restore 语义，避免误伤当前治理流程。 |
| A6 | SI 转项目并发与绑定缺口 | 部分修复 | 增加 `sourceSiId` 唯一约束、P2002 翻译、活动绑定校验，并批量收回同 SI 其他预发；上线前需清洗重复项目。 |
| A7 | 用户角色变更无护栏 | 待专项 | 需补最后管理员、活动绑定、在管项目和活动预发检查。 |
| A8 | 注册/治理/改邮箱 P2002 翻译缺失 | 部分修复 | 公开注册已补唯一冲突翻译；治理和账号改邮箱仍需补齐。 |
| A9 | 可删除带活动预发的 SI | 待专项 | 需决定“禁止删除”还是“事务内收回并通知”策略。 |

## 编辑器与协作

| 编号 | 问题 | 状态 | 本轮措施 / 后续措施 |
| --- | --- | --- | --- |
| B1 | 无未保存离开保护，冲突后继续输入必丢 | 部分修复 | 增加 `beforeunload` 提示；锁冲突后编辑器切只读。路由内跳转拦截和复制逃生门待补。 |
| B2 | 自动保存失败无限重试 | 部分修复 | 连续失败 3 次后暂停自动重试并提示。更完整的手动重试 UI 待补。 |
| B3 | flushAutoSave 与 submit 竞态 | 已修复 | 等待 in-flight 保存后再次取消 debounce，避免遗留定时器并发提交。 |
| B4 | 插件状态跨 kind 复用 lastId | 待专项 | 需扩展 plugin state 的 kind 判定。 |
| B5 | 跨段落标删拆多条修订 | 待专项 | 需引入 groupId 级别的一次操作聚合。 |
| B6 | 只读状态侧栏仍可改文档 | 待专项 | 需按 `editor.isEditable` 和 createdBy 权限禁用侧栏动作。 |
| B7 | 追踪关闭时继承他人 revision mark | 待专项 | 需在关闭追踪时过滤输入继承 mark。 |
| B8 | 历史工作视图渲染 plainText | 待专项 | 需只读 Tiptap 渲染 contentJson。 |
| B9 | 后端信任前端投影 | 待专项 | 同 A4，服务端重导投影。 |
| B10 | 每次击键多次全文档 O(n) | 待专项 | 需性能专项：延迟投影、版本计数、DecorationSet 增量映射。 |
| B11 | 批注随文本删除湮灭 | 待专项 | 需批注实体外置或删除确认。 |

## 前端交互

| 编号 | 问题 | 状态 | 本轮措施 / 后续措施 |
| --- | --- | --- | --- |
| C1 | AdminDashboard/review 页竞态 | 待专项 | 建议统一数据获取层或补 cancelled guard。 |
| C2 | 看板深链筛选失效 | 待专项 | 需项目/SI 列表解析 searchParams 并统一 overdue 口径。 |
| C3 | 筛选不进 URL、列表无分页 | 待专项 | 需服务端分页和 URL 化筛选。 |
| C4 | 确认交互三套并存 | 部分修复 | 章节删除已接 Dialog；全局 ConfirmDialog 待统一。 |
| C5 | 无 toast 系统 | 待专项 | 建议引入统一 toast，替换散落 banner。 |
| C6 | 顶栏假交互 | 部分修复 | 移除无效搜索/语言切换，通知红点接真实 unreadCount。完整全局搜索待产品定义。 |
| C7 | 永久 disabled 占位按钮 | 待专项 | SI 归档/删除、章节重排说明等需接线或移除。 |
| C8 | 报表统计维度下拉无效 | 待专项 | 需接入维度查询或删除控件。 |
| C9 | `/admin/audit` 无导航入口 | 已修复 | 管理员导航新增“审计日志”。 |
| C10 | 表单客户端校验薄弱 | 部分修复 | 章节号新增客户端整数校验；用户/改密/注册更多表单待补。 |
| C11 | PasswordResetGuard 仅客户端兜底 | 待专项 | 需 middleware 或服务端 pathname 注入后做服务端 redirect。 |

## 安全与工程

| 编号 | 问题 | 状态 | 本轮措施 / 后续措施 |
| --- | --- | --- | --- |
| D1 | 注册/忘记密码无限流 | 已修复 | 复用内存限流器，公开入口按 IP 返回 429 + Retry-After。 |
| D2 | seed 弱密码 + 无生产防护 | 已修复 | 生产必须 `--force`；支持 `SEED_PASSWORD_<USERNAME>` 覆盖默认密码。 |
| D3 | `SESSION_SECRET` 与 `jose` 死配置/死依赖 | 部分修复 | 删除 `.env.example` 的 `SESSION_SECRET` 和直接 `jose` 依赖；锁文件里可能仍保留传递依赖。 |
| D4 | 临时密码熵偏弱 | 已修复 | 管理员重置临时密码从 40 bit 提升到 64 bit。 |
| D5 | 会话无滑动续期、过期记录无清理 | 待专项 | 需会话策略和清理任务设计。 |
| D6 | 无 middleware/security headers | 待专项 | 需补 middleware、headers 和 Origin 校验。 |
| D7 | 分页覆盖率低 | 待专项 | 同 C3，增长型列表需分页。 |
| D8 | 编辑器测试基建未跑真实插件 | 待专项 | 需 jsdom 集成测试，避免本轮止血和测试环境升级混在一起。 |
| D9 | 构建产物入库 | 已修复 | `.gitignore` 增加 `next-env.d.ts`、`tsconfig.tsbuildinfo`，并从 Git 索引移除。 |

## 补充审核发现

| 问题 | 状态 | 本轮措施 / 后续措施 |
| --- | --- | --- |
| 项目指派后活动草稿/待办仍归旧负责人 | 已修复 | 指派事务内迁移 author/editor 活动草稿 owner，并迁移 `doc_return` / `doc_review` 打开待办。 |
| 章节排序前端 POST、后端 PATCH | 已修复 | 前端改为 PATCH。 |
| 章节号非法输入吞成 null，且无数据库唯一兜底 | 已修复 | 前端正整数校验；新增 `chapter_no_key` 唯一键和迁移。 |
| lint 脚本不可用 | 已修复 | 新增 ESLint flat config 和依赖，`npm run lint -- --max-warnings=0` 已通过。 |
| Tiptap 子包版本不一致导致类型冲突 | 已修复 | Tiptap 依赖统一到 3.26.1。 |
