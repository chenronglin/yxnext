# 审计整改跟踪（2026-06-13）

本文件逐条确认 `docs/AUDIT-2026-06-13.md` 与补充审核发现。状态说明：

- `已修复`：本轮已完成代码/迁移/测试整改。
- `部分修复`：已完成止血或低风险部分，仍需专项继续。
- `待专项`：需要较大架构改造、数据清洗、产品决策或更多测试基建，不应混入本轮止血补丁。

## 严重问题

| 编号 | 问题 | 状态 | 本轮措施 / 后续措施 |
| --- | --- | --- | --- |
| S1 | 作者可越权读取任意作者预发记录 | 已修复 | `listSiPreissues` 禁止作者端 `authorId` 覆盖本人过滤，并新增回归测试。 |
| S2 | `active_pair_key` 语义错误 | 已修复 | 活动绑定唯一键改为作者维度，新增迁移回填活动键。上线前需先清洗存量重复活动绑定。 |
| S3 | 修订模式 Delete 卡死 | 已修复 | Delete 后光标推进到标删区段之后，并跳过紧邻 deleted 文本。 |
| S4 | 缺少接受/拒绝修订机制 | 待专项 | 需要新增 revision accept/reject 命令、侧栏交互和提交前拦截，涉及编辑器核心状态机。 |
| S5 | replace 误把 inserted 标成 original | 已修复 | 直接输入替换分段处理，IME 替换过滤 inserted slice，关闭追踪时也过滤继承 revision mark。 |
| S6 | 结构性编辑绕过修订追踪 | 部分修复 | 已补 Delete/Backspace、粘贴、输入关闭追踪过滤与批注删除保护；Enter/cut/drop 等结构事务仍需在 S4 专题前后继续收敛。 |
| S7 | 移动端无导航入口 | 已修复 | 小屏汉堡按钮打开移动抽屉，复用同一份角色导航。 |
| S8 | 驳回页展示虚假管理员反馈 | 已修复 | 删除硬编码反馈文案，仅保留通用驳回说明。 |
| S9 | SI 主类型参数与表单脱节 | 已修复 | 新增 `/api/si-main-types` 只读接口，SI 表单和列表筛选改用启用主类型。 |
| S10 | 删除章节无确认弹窗 | 已修复 | 章节删除改为确认 Dialog，并展示章节标题和后果说明。 |
| S11 | 401 无全局处理、编辑侧路由缺守卫 | 已修复 | `fetchJson` 统一 401 跳登录；`/si` 与 `/review` 增加服务端角色守卫；强制改密用户由服务端 layout 重定向到 `/settings`。 |
| S12 | `ignoreBuildErrors: true` | 已修复 | 移除该配置，新增 `typecheck` 脚本；生产构建已确认会执行 TypeScript。 |

## 中等问题

| 编号 | 问题 | 状态 | 本轮措施 / 后续措施 |
| --- | --- | --- | --- |
| A1 | chapter 阶段计划永不 completed | 已修复 | 解锁质检时同步完成正文阶段计划。 |
| A2 | complete/archive/cancel 不清理打开待办，统计不过滤生命周期 | 已修复 | 项目完成/归档/取消时取消打开待办；逾期统计过滤 active 项目；恢复时按当前 Doc 状态重建必要阶段状态。 |
| A3 | 逾期同步在读路径全量扫描且并发通知 | 已修复 | 读路径已移除同步写入，新增 `POST /api/cron/project-timeline-sync`，通过 `CRON_SECRET` 保护并只在状态真实翻转时通知。 |
| A4 | cleanText 校验被回退顺序架空 | 已修复 | 保存时服务端基于 `contentJson` 重导 `plainText/cleanText/exportText/summary/count`，前端投影不再作为可信来源。 |
| A5 | 治理生命周期缺状态机 | 已修复 | 新增显式生命周期转移表：`active -> completed/archive/cancel`、`completed -> archive`、`archived/cancelled -> active restore`，非法转移稳定返回 409。 |
| A6 | SI 转项目并发与绑定缺口 | 已修复 | 增加 `sourceSiId` 唯一约束、P2002 翻译、活动绑定校验，并批量收回同 SI 其他预发；上线前重置数据库覆盖存量清洗。 |
| A7 | 用户角色变更无护栏 | 已修复 | 最后管理员不可降级/禁用；存在活动绑定、活动项目、活动预发或待处理稿件时禁止改成不兼容角色。 |
| A8 | 注册/治理/改邮箱 P2002 翻译缺失 | 已修复 | 公开注册、治理用户保存、账号邮箱修改均捕获 P2002 并返回稳定业务错误。 |
| A9 | 可删除带活动预发的 SI | 已修复 | 产品决策采用“事务内收回并通知”：删除 SI 时收回活动预发、清理 dedupe key、关闭待办、通知作者、写审计日志。 |

## 编辑器与协作

| 编号 | 问题 | 状态 | 本轮措施 / 后续措施 |
| --- | --- | --- | --- |
| B1 | 无未保存离开保护，冲突后继续输入必丢 | 部分修复 | 增加 `beforeunload` 提示；锁冲突后编辑器切只读。路由内跳转拦截和复制逃生门待补。 |
| B2 | 自动保存失败无限重试 | 部分修复 | 连续失败 3 次后暂停自动重试并提示。更完整的手动重试 UI 待补。 |
| B3 | flushAutoSave 与 submit 竞态 | 已修复 | 等待 in-flight 保存后再次取消 debounce，避免遗留定时器并发提交。 |
| B4 | 插件状态跨 kind 复用 lastId | 已修复 | revision plugin state 新增 `lastKind`，复用上一修订必须匹配 kind。 |
| B5 | 跨段落标删拆多条修订 | 已修复 | 同一次删除操作共享 `groupId`，侧栏按 `groupId` 聚合展示并可整体定位/清理。 |
| B6 | 只读状态侧栏仍可改文档 | 已修复 | `DiscussionSidebar` 按 `editor.isEditable` 禁用删除标记，移除只读写事务入口。 |
| B7 | 追踪关闭时继承他人 revision mark | 已修复 | 关闭追踪时的输入/粘贴也走过滤 revision mark 的插入路径。 |
| B8 | 历史工作视图渲染 plainText | 已修复 | 历史详情页使用只读 Tiptap 渲染 `contentJson`，并使用独立只读文案。 |
| B9 | 后端信任前端投影 | 已修复 | 同 A4，服务端统一重导投影。 |
| B10 | 每次击键多次全文档 O(n) | 部分修复 | 侧栏刷新已 debounce，投影保留必要 autosave debounce；Decoration/投影增量化仍建议在大文档性能专项中继续压测。 |
| B11 | 批注随文本删除湮灭 | 部分修复 | 删除含批注选区前增加确认保护；批注实体外置仍属于后续模型升级。 |

## 前端交互

| 编号 | 问题 | 状态 | 本轮措施 / 后续措施 |
| --- | --- | --- | --- |
| C1 | AdminDashboard/review 页竞态 | 待专项 | 建议统一数据获取层或补 cancelled guard。 |
| C2 | 看板深链筛选失效 | 已修复 | 项目、治理项目、SI、预发列表均解析 searchParams；旧 `overdue=1` 兼容并规范化。 |
| C3 | 筛选不进 URL、列表无分页 | 已修复 | 项目/SI/预发列表改为 URL 化筛选和服务端分页，默认 `pageSize=20`。 |
| C4 | 确认交互三套并存 | 已修复 | 新增全局 `AppFeedbackProvider`，危险操作统一走 `useConfirmDialog`；编辑器插件内部保留批注删除兜底确认。 |
| C5 | 无 toast 系统 | 已修复 | 新增全局 toast，SI 归档/删除/回退/预发等即时操作使用统一反馈。 |
| C6 | 顶栏假交互 | 已修复 | 移除无效搜索/语言切换，通知红点接真实 unreadCount；当前版本不提供全局搜索入口。 |
| C7 | 永久 disabled 占位按钮 | 部分修复 | SI 列表/详情归档删除、SI 版本回退已接线；无产品定义的占位入口继续按页面逐步清理。 |
| C8 | 报表统计维度下拉无效 | 已修复 | 当前版本不提供维度统计，报表页已移除“统计维度”下拉，仅保留时间范围。 |
| C9 | `/admin/audit` 无导航入口 | 已修复 | 管理员导航新增“审计日志”。 |
| C10 | 表单客户端校验薄弱 | 部分修复 | 章节号新增客户端整数校验；用户/改密/注册更多表单待补。 |
| C11 | PasswordResetGuard 仅客户端兜底 | 已修复 | middleware 注入当前路径，应用服务端 layout 对强制改密用户重定向到设置页。 |

## 安全与工程

| 编号 | 问题 | 状态 | 本轮措施 / 后续措施 |
| --- | --- | --- | --- |
| D1 | 注册/忘记密码无限流 | 已修复 | 复用内存限流器，公开入口按 IP 返回 429 + Retry-After。 |
| D2 | seed 弱密码 + 无生产防护 | 已修复 | 生产必须 `--force`；支持 `SEED_PASSWORD_<USERNAME>` 覆盖默认密码。 |
| D3 | `SESSION_SECRET` 与 `jose` 死配置/死依赖 | 部分修复 | 删除 `.env.example` 的 `SESSION_SECRET` 和直接 `jose` 依赖；锁文件里可能仍保留传递依赖。 |
| D4 | 临时密码熵偏弱 | 已修复 | 管理员重置临时密码从 40 bit 提升到 64 bit。 |
| D5 | 会话无滑动续期、过期记录无清理 | 已修复 | 会话读取低于阈值时滑动续期；Cron 接口同步清理过期/撤销会话。 |
| D6 | 无 middleware/security headers | 已修复 | 新增 middleware，设置安全响应头，并对 mutating API 做 Origin 校验；本地/脚本无 Origin 请求继续交给鉴权或 Cron 密钥。 |
| D7 | 分页覆盖率低 | 已修复 | 项目、治理项目、SI、预发记录已服务端分页；其他小体量后台列表保持现状。 |
| D8 | 编辑器测试基建未跑真实插件 | 部分修复 | 新增跨段落删除 group 聚合单测，保留现有 Tiptap 插件级测试；jsdom 真实浏览器式集成测试仍建议后续单独升级。 |
| D9 | 构建产物入库 | 已修复 | `.gitignore` 增加 `next-env.d.ts`、`tsconfig.tsbuildinfo`，并从 Git 索引移除。 |

## 补充审核发现

| 问题 | 状态 | 本轮措施 / 后续措施 |
| --- | --- | --- |
| 项目指派后活动草稿/待办仍归旧负责人 | 已修复 | 指派事务内迁移 author/editor 活动草稿 owner，并迁移 `doc_return` / `doc_review` 打开待办。 |
| 章节排序前端 POST、后端 PATCH | 已修复 | 前端改为 PATCH。 |
| 章节号非法输入吞成 null，且无数据库唯一兜底 | 已修复 | 前端正整数校验；新增 `chapter_no_key` 唯一键和迁移。 |
| lint 脚本不可用 | 已修复 | 新增 ESLint flat config 和依赖，`npm run lint -- --max-warnings=0` 已通过。 |
| Tiptap 子包版本不一致导致类型冲突 | 已修复 | Tiptap 依赖统一到 3.26.1。 |
