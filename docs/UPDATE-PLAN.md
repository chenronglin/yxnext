# 悦享小说创作平台基础改造 · 实施计划（UPDATE-PLAN）

| 项目 | 内容 |
| --- | --- |
| 依据文档 | `docs/悦享小说创作平台技术升级方案.md`（技术附件 V2.0） |
| 编制日期 | 2026-09-02 |
| 代码基线 | 分支 `main`，提交 `3515025` |
| 实施范围 | 现有小说创作平台增量改造 |
| 实施资源 | 1 名资深全栈工程师 + AI 辅助编程 |
| 工期与工作量 | 20 个工作日 / 6 个工作包 / 20 人日 |

> 本文把技术附件转换为可执行的工程任务、代码落点、测试清单和交付门槛。  
> 如本文与技术附件冲突，以技术附件为准；任何超出四项基础改造的内容必须另行书面确认。

---

## 0. 计划约束

1. 本期只实施 SI 信息扩展、正文第 0 章、批注回复、工作日计划，以及四项功能必需的数据库变更、权限校验、回归测试、部署和使用说明。
2. 继续沿用现有 Next.js、Prisma/MySQL、Tiptap、角色权限、稿件版本、质检和导出流程，不进行整体重构。
3. 存量 SI 和存量项目不强制补录或迁移；新增字段应允许空值，存量项目无第 0 章时仍可完成原流程。
4. 新功能不得新增消息通知、外部消息推送或新的第三方系统依赖。
5. 数据库唯一性、权限校验、业务写入和操作日志应在服务层及事务内统一处理，不只依赖前端限制。
6. 每个工作包完成后立即补充测试并执行质量门禁，不把全部测试集中到最后三天。
7. 2026 年 8 月 18 日会议纪要用于核对需求来源和既有业务口径；实施范围仍以技术附件 V2.0 为准。会议纪要与技术附件不一致时，不得直接扩大本期范围，应先形成书面确认或变更。

## 1. 现有代码基线

### 1.1 可复用能力

| 能力 | 现有落点 | 本期复用方式 |
| --- | --- | --- |
| SI 与版本 | `server/modules/si/si.service.ts`、`StoryIdea`、`StoryIdeaVersion`、`SiPreissue` | 扩展字段、快照、预发布和回滚，不重写 SI 状态流 |
| 章节与质检 | `server/modules/project/project.service.ts`、`Doc`、`DocRevision`、`ReleaseSourceRevision` | 放开并保护第 0 章，复用提交、退回、定稿、质检和导出链路 |
| 批注 | `components/doc/tiptap/extensions.tsx`、`discussion-sidebar.tsx` | 批注继续保存在稿件 JSON；回复使用独立数据表和接口 |
| 阶段计划 | `ProjectStagePlan`、`StagePlanDefault`、阶段预警定时任务 | 增加工作日日历、日期互算和修改历史，保留原阶段状态机 |
| 权限与审计 | 当前用户、项目可见性、`OperationLog`、`lib/audit-log.ts` | 所有新增接口复用现有角色和项目归属校验 |
| 测试与发布 | Vitest、TypeScript、ESLint、Next.js Build、Prisma Migration | 在现有工具链内增加测试和发布步骤 |

### 1.2 已确认的改造点

| 项目 | 当前情况 | 必须处理 |
| --- | --- | --- |
| SI 信息 | 现有模型没有本期四个新增字段，快照和回滚仅处理旧字段；用户输入标签后自动留痕、下次可选属于既有能力 | 扩展模型、字典/配置、表单、详情、快照、预发布、版本和回滚；保持既有标签模式不变并执行回归 |
| 第 0 章 | 数据库无符号整数字段可保存 0，但接口和服务使用正整数或 `<= 0` 校验；部分页面用真值判断章节号 | 统一允许 0，增加唯一性和固定首位规则，修正所有显示、排序和导出路径 |
| 批注回复 | 批注存在稿件 JSON 的 comment mark 中，没有独立回复模型 | 新增回复表、服务、接口和侧栏交互；不得改写稿件 JSON 或生成新版本 |
| 工作日计划 | 当前多处使用自然日加法，阶段计划缺少统一工作日日历和修改历史；会议纪要记载原修改权限仅向管理员开放 | 新增例外日期和统一纯函数，替换自然日算法，增加留痕；修改权限按开工前书面确认的角色口径实施 |

### 1.3 必须继续遵守的工程约定

- BigInt ID 的接口出参统一转字符串，入参在服务层校验后转换。
- 业务错误使用稳定错误码和现有统一错误响应，不向前端暴露 Prisma 原始错误。
- 唯一性依靠数据库唯一约束兜底；并发冲突转换为明确业务错误。
- 业务写入、关联变更和操作日志尽量位于同一事务。
- 用户可见文案同步维护中英文 i18n 键，不新增硬编码文案告警。
- Migration 使用独立目录和 SQL 文件，写明变更原因、存量兼容和回滚方式。

## 2. 开工前确认项

以下事项须在第 1 个工作日确认；无法确认的事项进入问题清单，可能影响对应工作包：

| 确认项 | 必须明确的内容 | 影响 |
| --- | --- | --- |
| SI 现有标签 | 确认继续保持“用户输入后自动留痕、下次可选”，不改为统一预设标签 | SI 回归范围，原则上不产生新增开发 |
| SI 类型 | 选项、排序、是否必填、停用规则 | 数据模型、字典和表单 |
| 创作难度 | 选项、排序、是否必填、停用规则 | 数据模型、字典和表单 |
| 参考书籍 | 标题与链接是否独立、URL 协议和长度 | 表单与接口校验 |
| 第 0 章 | 创建入口、默认标题能否修改、谁可创建和删除 | 页面、接口和权限 |
| 批注回复 | 最大长度、展示顺序、批注失效判定 | 数据字段、接口和 UI |
| 日期口径 | 起止日是否计入、非工作日起始日如何处理、阶段重叠是否仅提示 | 工作日纯函数与接口 |
| 时间修改权限 | 技术附件约定“编辑可调整本人负责且未完成阶段”，会议纪要则记载原权限仅向管理员开放；甲方须书面确认本期最终角色矩阵 | 阶段计划接口、页面和验收；未确认前不得开放生产写权限 |
| 日历数据 | 当前年度及后续年度节假日、调休工作日、维护人 | 日历初始化与后台维护 |
| 环境与数据 | 测试/生产权限、备份回滚方式、脱敏 SI/章节/批注/跨年计划数据 | 联调、回归和发布 |

默认不替甲方猜测未确认的业务规则。若必须先行开发，须把临时口径记录到确认单并由双方书面确认。

### 2.1 会议纪要四项需求覆盖核对

| 会议纪要需求点 | 技术附件 V2.0 状态 | 本计划处理 | 覆盖结论 |
| --- | --- | --- | --- |
| SI：保留用户输入标签自动留痕、下次可选的现有模式 | 实施原则要求保留既有流程，但未单独点名标签行为 | 纳入 WP2/WP6 回归，不改造为统一预设标签 | **覆盖：既有能力保持** |
| SI：增加 SI 类型、创作难度、参考书籍标题和链接 | 第 3.1 条明确列入 | 按第 3.2、8、9 章开发、测试和验收 | **覆盖** |
| 正文：章节序号支持 0，第 0 章用于填写书籍整体介绍说明，方便运营交接 | 第 3.2 条明确列入 | 按第 3.3、8、9 章完成全链路适配 | **覆盖** |
| 正文：直接粘贴并显示 Word/WPS 表格，失败时再考虑附件 | 第 6 条明确排除编辑器表格、Office/WPS 表格粘贴；附件也不在四项范围内 | 本期已确认不实施，不进入任务、工期和验收；如恢复需求，另行书面变更 | **本期不覆盖（已确认排除）** |
| 正文：持有人逻辑说明 | 技术附件未将持有人规则调整列为新增功能 | 按既有平台现状继续运行，不作为本期独立开发、专项测试或验收项 | **不属于本期改造需求** |
| 批注：直接在编辑批注下进行一级回复，无需提交新版本 | 第 3.3 条明确列入 | 按第 3.4、8、9 章开发、测试和验收 | **覆盖** |
| 项目时间：自定义起止时间，支持工作日、节假日和调休 | 第 3.4 条明确列入 | 按第 3.5、8、9 章开发、测试和验收 | **覆盖** |
| 项目时间：防止修改已完成阶段，明确修改权限 | 已完成阶段保护已列入；附件采用编辑有限写入、作者只读口径，与会议纪要记载的原管理员权限存在差异 | WP1 取得甲方书面角色矩阵，WP5 按最终书面口径实现，WP6 验收 | **功能覆盖，权限待书面确认** |

据此，四个主题均已进入需求核对，但不能表述为会议纪要全部细项均在固定价范围内：正文板块本期仅实施第 0 章，表格/附件替代已确认排除，持有人规则不作调整；时间修改角色权限须在开工前确认。

## 3. 目标设计

### 3.1 数据库变更

计划通过一批可回滚 Migration 完成以下增量结构，最终字段名以详细设计为准：

| 结构 | 主要内容 | 兼容策略 |
| --- | --- | --- |
| SI 字典/配置 | 以类别区分“SI 类型”和“创作难度”，保存编码、名称、排序和启停状态 | 不修改现有主类型；停用项保留历史可读性 |
| `StoryIdea` | 增加 SI 类型、创作难度、参考书籍标题、参考书籍链接 | 新字段可空，存量记录无需回填 |
| `DocCommentReply` | 文档 ID、批注 ID、回复作者、纯文字内容、创建时间 | 与稿件版本分离，不修改现有 JSON |
| `WorkdayException` | 日期、是否工作日、名称/说明、维护人和更新时间 | 仅保存周末规则的例外日期 |
| `ProjectStagePlan` | 计划开始日、计划结束日；现有 `planDays` 统一解释为计划工作日数 | 存量字段先兼容读取，不批量覆盖实际时间 |
| `ProjectStagePlanChange` | 阶段计划、修改人、原因、修改前后值和时间 | 只记录本期上线后的调整 |

数据库约束：

- 同一字典类别下编码唯一。
- 同一文档、批注和回复 ID 唯一；按文档、批注、创建时间建立查询索引。
- 工作日例外日期唯一。
- 阶段计划历史只追加，不覆盖旧记录。

### 3.2 SI 信息扩展

实施任务：

1. 扩展 Prisma 模型、Migration、共享类型和 Zod 校验。
2. 在现有后台参数页增加 SI 类型和创作难度的最小维护入口，复用现有参数页交互。
3. 扩展 SI 新建、编辑、详情和版本页面。
4. 修改 `makeSnapshot()` 及创建、更新、标题更新、预发布和回滚路径，快照同时保存字典编码与名称。
5. 旧快照缺少新字段时按空值解析，不产生异常。
6. 参考书籍链接只允许双方确认的安全协议，并限制长度。
7. 回归现有 SI 标签行为，保证用户输入标签后仍可留痕并在下次选择，不引入统一预设标签或强制迁移。

完成条件：

- 四个字段可保存和展示。
- 版本快照、预发布快照和回滚结果一致。
- 字典项停用后历史版本仍可显示原名称。
- 存量 SI 无新字段时可继续原流程。
- 现有标签的录入、留痕和再次选择行为不因新增字段发生变化。

### 3.3 正文第 0 章

本工作包仅把现有从第 1 章开始的章节序号扩展为支持第 0 章；第 0 章用于填写书籍整体介绍说明，方便向运营交接内容，不包含表格能力或其他正文功能改造。

实施任务：

1. 将章节创建、修改 Route 的章节号校验由正整数改为大于等于 0 的整数。
2. 修改项目服务中 `chapterNo <= 0` 校验，同时保留章节号唯一约束。
3. 明确第 0 章的唯一键、创建、删除和重排规则；普通章节重排不得把第 0 章移出首位。
4. 修复页面中 `chapter.chapterNo ? ... : ...` 等真值判断，统一使用显式空值判断。
5. 核对章节导航、开放内容接口、清稿、质检生成、重新质检和最终导出排序。
6. 为并发重复创建第 0 章提供稳定错误码，不返回数据库原始异常。

重点文件：

- `app/api/projects/[projectId]/chapters/route.ts`
- `app/api/projects/[projectId]/chapters/[docId]/route.ts`
- `server/modules/project/project.service.ts`
- `lib/chapter-order.ts`
- `components/project/project-detail.tsx`
- `components/doc/chapter-navigator.tsx`
- `server/modules/open/open-project.service.ts`
- `server/modules/open/open-content.service.ts`

完成条件：

- 同一项目只能创建一个第 0 章。
- 第 0 章固定显示在正文最前。
- 第 0 章完整通过保存、提交、退回、批注、版本、定稿、质检、清稿和导出链路。
- 无第 0 章的存量项目行为不变。

### 3.4 批注回复

实施任务：

1. 新增 `DocCommentReply` 模型、Migration、共享类型和输入校验。
2. 新建回复服务，复用现有文档可见性和项目角色校验：
   - 仅项目作者可新增；
   - 仅允许回复项目编辑创建且当前仍有效的批注；
   - 内容为非空纯文字并执行最大长度限制；
   - 查询按约定顺序返回历史回复。
3. 新增接口：
   - `GET /api/docs/[docId]/comments/[commentId]/replies`
   - `POST /api/docs/[docId]/comments/[commentId]/replies`
4. 扩展 `discussion-sidebar.tsx`，在批注项下展示回复列表和输入区；无权限或批注失效时只读。
5. 批注失效时保留历史回复并展示状态，不物理删除回复。
6. 回复写入不得更新 `contentJson`、`lockVersion` 或 `DocRevision`，不得新增通知或待办。
7. 新增回复创建操作日志及中英文文案。

完成条件：

- 作者、编辑、管理员的读取和写入权限符合约定。
- 有效批注可回复，失效批注不可新增回复。
- 刷新、切换文档或查看历史后回复仍存在。
- 回复不改变稿件内容哈希、版本号和自动保存状态。

### 3.5 工作日计划

实施任务：

1. 新增工作日纯函数：
   - `isWorkday(date, exceptions)`
   - `countWorkdays(start, end, exceptions)`
   - `addWorkdays(start, count, exceptions)`
   - `previousWorkday(date, exceptions)`
2. 纯函数不访问数据库，统一使用明确时区和日期格式；覆盖周末、节假日、调休、跨月和跨年。
3. 新增工作日例外日期的管理员维护接口和最小管理界面，支持按年份查询和批量保存。
4. 扩展项目阶段计划，权限以 WP1 书面确认的最终角色矩阵为准；未另行确认时以技术附件为合同基线：
   - 编辑仅可修改本人负责、活动项目的未完成阶段；
   - 作者只读；
   - 已完成阶段以及 `startedAt`、`completedAt` 等实际时间不可被计划修改覆盖；
   - 修改起止日时重算工作日数；
   - 修改工作日数时重算结束日；
   - 每次修改必须填写原因并保存前后值。
5. 将现有自然日算法统一切换到工作日函数，重点核对：
   - `server/modules/si/si.service.ts` 创建项目初始计划；
   - `server/modules/project/project.service.ts` 阶段解锁和截止日；
   - `server/modules/doc/doc.service.ts` 阶段推进；
   - `server/modules/admin/admin.service.ts` 管理端计划调整；
   - `server/shared/project-stage-timeline.ts` 到期和提前预警。
6. 阶段重叠按照双方确认口径返回提示，不改变原有阶段解锁和完成状态机。
7. 预警继续使用现有通知类型，只替换日期计算口径，不新增通知类型。

完成条件：

- 起止日和工作日数能够双向计算。
- 周末、节假日、调休和跨年结果正确。
- 编辑、作者和管理员权限边界与 WP1 书面确认的角色矩阵一致。
- 修改历史可追溯，实际时间不被覆盖。
- 定时预警与页面截止日期使用同一工作日口径。

## 4. 20 个工作日排期

| 工作包 | 时间 | 人日 | 主要产出 | 退出门槛 |
| --- | --- | ---: | --- | --- |
| WP1 需求确认、基线核对与技术设计 | 第 1 日～第 2 日上午 | 1.5 | 确认单、会议纪要差异清单、数据设计、Migration/回滚方案、测试清单 | 第 2 章事项已确认或形成书面问题清单；时间权限已有书面角色矩阵 |
| WP2 SI 信息扩展 | 第 2 日下午～第 4 日 | 2.5 | 数据结构、字典/配置、页面、快照和回滚 | SI 专项测试通过，标签留痕及存量 SI 冒烟通过 |
| WP3 正文第 0 章 | 第 5 日～第 8 日上午 | 3.5 | 全链路第 0 章支持 | 创建、排序、流程、质检和导出测试通过 |
| WP4 批注回复 | 第 8 日下午～第 11 日 | 3.5 | 回复模型、接口、侧栏和历史留存 | 权限、失效和不触发版本测试通过 |
| WP5 工作日计划 | 第 12 日～第 17 日 | 6 | 日历、日期互算、修改历史、书面角色矩阵权限和预警适配 | 纯函数、服务、权限和跨年测试通过 |
| WP6 集成回归、上线与文档 | 第 18 日～第 20 日 | 3 | 全量回归、部署、回滚说明和使用说明 | 第 8 章质量门禁及第 9 章验收映射全部通过 |
| **合计** |  | **20** |  |  |

执行说明：

- 每个工作包结束时提交可独立验证的代码和测试，不跨包积压未完成事项。
- 第 17 日完成开发冻结；第 18～20 日只处理回归缺陷、部署和文档，不接受未走变更流程的新功能。
- 发生阻塞时记录阻塞起止时间、责任方和恢复条件；符合合同顺延条件的，工期按实际影响调整。

## 5. API 与页面落点

| 功能 | 接口/服务 | 页面 |
| --- | --- | --- |
| SI 字典 | 管理端字典查询、新增、修改、启停接口；复用 admin service 结构 | 现有管理员参数页 |
| SI 字段 | 现有 `/api/si`、`/api/si/[siId]`、版本和回滚接口扩展 | SI 新建、编辑、详情、版本页 |
| 第 0 章 | 现有章节创建、修改、排序、质检、导出接口扩展 | 项目详情、章节目录、编辑器、清稿页 |
| 批注回复 | `GET/POST /api/docs/[docId]/comments/[commentId]/replies` | 现有讨论侧栏 |
| 工作日日历 | 管理端例外日期查询与维护接口 | 现有管理员参数页新增日历区域 |
| 阶段计划 | 项目阶段计划读取、修改和历史查询接口 | 项目阶段计划表及修改历史入口 |

接口统一要求：

- Route 只处理身份、参数解析和响应封装，业务规则放入 service。
- 写接口执行角色、项目归属和生命周期状态校验。
- 错误码、i18n 文案和前端反馈同时落地。
- 更新阶段计划时使用并发保护，避免后写入覆盖先写入。

## 6. 代码变更矩阵

| 文件/目录 | 计划改动 |
| --- | --- |
| `prisma/schema.prisma` | SI 新字段和字典、批注回复、工作日例外、阶段计划字段与修改历史 |
| `prisma/migrations/*` | 本期增量 SQL、索引、唯一约束和回滚说明 |
| `server/modules/si/si.service.ts` | SI 校验、快照、预发布、版本、回滚和初始阶段计划工作日计算 |
| `components/si/*`、`types/si.ts` | SI 表单、详情、版本和类型扩展 |
| `server/modules/project/project.service.ts` | 第 0 章规则、排序、质检/导出适配和工作日截止日 |
| `app/api/projects/[projectId]/chapters/**` | 第 0 章参数校验 |
| `components/project/*`、`components/doc/chapter-navigator.tsx` | 第 0 章展示、阶段计划编辑和历史 |
| `server/modules/doc/comment-reply.service.ts` | 批注回复查询、创建、权限和有效性校验 |
| `app/api/docs/[docId]/comments/[commentId]/replies/route.ts` | 批注回复 GET/POST |
| `components/doc/tiptap/discussion-sidebar.tsx` | 回复列表、输入、失效和只读状态 |
| `lib/workday-calendar.ts` | 工作日纯函数 |
| `server/shared/workday-calendar.ts` | 日历读库和统一服务封装 |
| `server/modules/admin/admin.service.ts` | 日历维护和管理端阶段计划适配 |
| `server/modules/doc/doc.service.ts` | 阶段推进的工作日截止日 |
| `server/shared/project-stage-timeline.ts` | 工作日预警窗口 |
| `lib/audit-log.ts` | 新动作和字段中文标签 |
| `lib/i18n/locales/zh-CN.ts`、`en-US.ts` | 新增页面文案和 API 错误文案 |

实际实施前使用 `rg` 再次定位同类逻辑，避免只修改表中列出的单一调用点。

## 7. 数据迁移与回滚

### 7.1 上线前

1. 备份生产数据库并记录备份完成时间。
2. 在与生产结构一致的测试库执行 Migration。
3. 使用存量 SI、无第 0 章项目、带批注稿件和跨年阶段计划执行兼容验证。
4. 确认工作日日历数据已录入目标年度。
5. 保存上线前构建产物和数据库版本。

### 7.2 Migration 原则

- 新字段默认可空，不为存量记录制造伪数据。
- 新表上线时为空，不批量生成回复或历史记录。
- 不修改现有稿件 JSON。
- 不覆盖 `startedAt`、`completedAt` 等实际流程时间。
- 索引和唯一约束先在测试数据上检查潜在冲突。

### 7.3 回滚

- 应用回滚优先恢复到上线前版本。
- 数据库回滚脚本只移除本期尚未被生产业务使用的新增结构；若已产生新数据，先导出备份再执行。
- 如仅工作日日历配置错误，优先修正配置并重算未完成阶段，不回滚已完成阶段的实际记录。
- 回滚过程和结果写入部署记录。

## 8. 测试与质量门禁

### 8.1 新增测试

| 测试文件 | 覆盖内容 |
| --- | --- |
| `tests/si-extended-fields.test.ts` | 四字段校验、快照、预发布、版本、回滚、旧快照兼容、既有标签留痕不回归 |
| `tests/chapter-zero.test.ts` | 创建、并发唯一性、排序首位、重排保护、提交退回、质检和导出 |
| `tests/comment-reply.service.test.ts` | 作者权限、编辑批注校验、纯文字、失效、历史留存、不触发版本 |
| `tests/comment-reply-route.test.ts` | 身份、参数、错误码和响应结构 |
| `tests/workday-calendar.test.ts` | 周末、节假日、调休、跨月、跨年和双向计算 |
| `tests/stage-plan-workday.test.ts` | 书面角色矩阵权限、已完成阶段保护、修改历史、日期互算和重叠提示 |
| `tests/project-stage-timeline.test.ts` 扩展 | 工作日提前预警、到期和逾期状态 |

### 8.2 必须回归的现有能力

- SI 新建、编辑、预发布、转项目、归档、版本回滚，以及用户输入标签的留痕和再次选择。
- 普通章节创建、重排、提交、退回、定稿、重新质检和导出。
- 批注创建、编辑、定位、修订标记和自动保存。
- 项目阶段解锁、完成、预警、管理员治理页和作者只读视图。
- 管理员、编辑、作者三种角色的项目可见性和操作权限。

### 8.3 每个工作包的质量门禁

```bash
npm run lint
npm run typecheck
npm run test
npm run build
npm run check:i18n
```

要求：

- 新增和受影响测试全部通过。
- TypeScript、ESLint 和构建无新增错误。
- i18n 检查不增加新的硬编码文案告警。
- 数据库 Migration 可在空库和存量结构测试库执行。

## 9. 验收映射

| 技术附件验收项 | 工程验证 | 责任工作包 |
| --- | --- | --- |
| SI 新字段保存、展示、快照和回滚，存量不受影响 | SI 专项测试 + 现有标签留痕 + 存量 SI 手工回归 | WP2、WP6 |
| 第 0 章唯一且全链路顺序一致 | 第 0 章服务/路由测试 + 质检和导出手工回归 | WP3、WP6 |
| 批注回复权限、失效和历史留存正确，不触发版本 | 回复服务/路由测试 + 侧栏手工回归 | WP4、WP6 |
| 工作日处理周末、节假日、调休和跨年 | 纯函数与阶段计划测试 + 定时任务验证 | WP5、WP6 |
| 三类角色权限正确 | 按 WP1 书面角色矩阵执行权限回归 | WP2～WP6 |
| Migration、构建和测试通过 | 测试库演练 + 全量质量门禁 | WP1、WP6 |

验收演示最少包含：

1. 创建带四个新字段的 SI，保存版本后修改并回滚；同时验证原有标签可自动留痕并在下次选择。
2. 在存量项目创建第 0 章，完成提交、退回、定稿并进入质检和导出。
3. 作者回复编辑批注，验证刷新后留存、失效后只读且稿件版本不变。
4. 配置节假日和调休日期，验证跨年阶段的起止日、工作日数、历史和预警。
5. 使用管理员、编辑、作者账号按 WP1 书面角色矩阵分别验证权限边界。

## 10. 交付物

| 序号 | 交付物 | 完成标准 |
| --- | --- | --- |
| 1 | 四项基础改造功能 | 部署至现有系统，可按第 9 章演示和验收 |
| 2 | Prisma Schema、Migration 和回滚说明 | 测试环境演练通过，生产执行记录完整 |
| 3 | 新增及调整后的自动化测试 | 纳入 `tests/`，质量门禁通过 |
| 4 | 回归测试结果 | 记录测试范围、结果、遗留问题和处理结论 |
| 5 | 部署记录 | 包含版本、时间、执行人、Migration、配置和回滚点 |
| 6 | 使用说明 | 覆盖 SI 新字段、第 0 章、批注回复、日历维护和阶段计划调整 |

## 11. 范围边界

以下事项不进入任务清单、代码设计、工期或验收：

- 编辑器表格及 Office/WPS 表格粘贴（本期已确认不实施；会议纪要所述附件替代方案亦不实施）；
- 正文持有人规则调整；
- 第三方在线文档协作；
- 创作平台与翻译系统自动流转及翻译系统 API；
- 沟通记录、附件上传下载和对象存储；
- 新增消息通知、即时通讯推送、短信或邮件；
- 四项功能以外的整体重构、界面改版、性能专项优化和历史数据清洗迁移；
- 服务器、域名、SSL 证书及第三方服务采购。

如实施中发现上述内容是四项功能的前置依赖，应先记录风险并提交书面变更，不得直接扩大实现范围。

## 12. 风险与阻塞处理

| 风险 | 影响 | 处理方式 |
| --- | --- | --- |
| SI 字典和必填规则迟迟未确认 | WP2 无法定稿 | 第 1 日形成确认单；未确认则记录阻塞 |
| 第 0 章在隐藏路径仍按正整数处理 | 质检、开放接口或导出遗漏 | 使用 `rg` 全仓检索 `chapterNo` 并以端到端测试兜底 |
| 批注只存在 JSON，锚点可能随编辑失效 | 回复关联错误 | 创建回复时校验当前有效批注；历史回复独立保存 |
| 工作日算法分散 | 页面、服务和预警口径不一致 | 所有日期计算统一调用同一纯函数，不保留内联毫秒算法 |
| 节假日和调休数据不完整 | 截止日计算错误 | 甲方提供并确认年度数据；缺失年份禁止生成未经确认的结果 |
| 时间修改权限在会议纪要与技术附件中的口径不同 | 错误开放权限或验收争议 | WP1 由甲方书面确认最终角色矩阵；确认前不开放生产写权限，发生范围变化时同步变更文件 |
| 20 日内出现范围变更 | 压缩测试或造成延期 | 严格执行书面变更，重新评估人日和工期 |
| 生产结构与代码基线不一致 | Migration 或发布失败 | WP1 核对生产差异，先在同构测试库演练 |

## 13. 完成定义

本计划只有同时满足以下条件才视为完成：

- 四项功能全部达到第 9 章验收映射要求。
- 20 人日范围内约定的数据库、权限、测试、部署和说明工作全部完成。
- 新增及受影响测试、类型检查、Lint、构建和 i18n 检查通过。
- 存量 SI、普通章节、原批注和原阶段流程未出现阻断性回归。
- Migration 已在测试环境演练，生产发布和回滚点有记录。
- 甲方收到部署版本、回归结果和使用说明。
- 未完成事项已明确标注为范围外、待确认或书面变更，不以口头承诺留在本期。

## 14. 变更记录

| 日期 | 版本 | 说明 |
| --- | --- | --- |
| 2026-09-02 | v2.1 | 对照 2026-08-18 会议纪要补充覆盖矩阵和 SI 标签留痕回归，将时间修改权限列为书面确认项；按最新范围确认将正文改造限定为第 0 章，明确排除表格及持有人规则调整 |
| 2026-09-02 | v2.0 | 按技术附件 V2.0 重写为四项基础改造、20 个工作日、20 人日实施计划；移除旧版第三方集成及其他已取消范围 | 
