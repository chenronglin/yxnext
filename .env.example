# 数据库连接串示例：
# 请替换为你自己的数据库用户名、密码、主机、端口和库名，
# 严禁把真实凭据写回版本库。
DATABASE_URL="mysql://username:password@127.0.0.1:3306/database_name"

# 会话密钥示例：
# 生产环境必须替换成高强度随机字符串，
# 并且在真实密钥泄露后立即轮换。
SESSION_SECRET="replace-with-a-long-random-secret"
