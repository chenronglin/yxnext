# 数据库连接串示例：
# 请替换为你自己的数据库用户名、密码、主机、端口和库名，
# 严禁把真实凭据写回版本库。
DATABASE_URL="mysql://username:password@127.0.0.1:3306/database_name"

# 生产 HTTPS 部署保持 true 或留空；如果临时用 http://公网IP:端口 访问，需要显式设为 false。
SESSION_COOKIE_SECURE="true"

# Agent 项目只读接口专用 Bearer Token；生产环境必须使用至少 32 字节的随机值，并通过部署平台注入。
# 该 Token 只会被 /api/open/projects 识别，不可用于现有管理员或业务写接口。
OPEN_PROJECTS_API_TOKEN="replace-with-at-least-32-random-characters"
