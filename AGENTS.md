# 项目规范

## 编码

- 使用简体中文沟通；代码简洁高效，关键业务约束和非直观实现用中文注释。
- 修改前检查完整业务链路，保持既有权限、修订、批注和自动保存行为。
- React 状态变化须同步到 Tiptap/ProseMirror 实例；先启用修订，再开放编辑。不得依赖刷新或切章恢复正确行为。
- 状态切换和重渲染不得丢失未保存内容、修订或撤销历史，也不得触发意外保存。

## 测试

- 修复缺陷先补充可复现的回归测试，不得 mock 掉故障层或降低断言掩盖失败。
- 编辑器和流程变更必须验证“同一页面切换状态后立即操作”，不能只测试刷新后的初始状态。
- 按影响范围覆盖输入、删除、替换、粘贴、中文输入及批注；核对保存数据和重新打开后的结果。
- 编辑器/流程改动运行相关测试、类型检查、Lint 和构建；事务改动补充真实数据库并发及回滚验证。纯文案、样式修改按需检查。

## 数据与交付

- 使用隔离测试库或专用测试稿件，不覆盖真实业务内容；恢复数据前保留当前稿和历史快照。
- 数据库变更须核对目标环境及迁移状态；已应用的迁移不得改写，生成 Prisma Client 不等于执行迁移。
- 根据日志和实际错误定位问题，不将所有读取失败误报为权限不足。
- 交付说明实际修改、验证结果及未验证范围，明确区分本地修复、提交和部署。
